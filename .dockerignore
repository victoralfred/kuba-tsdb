# ==============================================================================
# Docker Build Context Exclusions
# Minimizes build context size for faster builds and smaller layers
# ==============================================================================

# Build artifacts (Rust/Cargo)
target/
debug/
release/
*.rlib
*.rmeta

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~
.*.sw?

# Version control
.git/
.gitignore
.gitattributes

# Documentation (not needed in production image)
*.md
!README.md
docs/

# Test files and coverage
coverage/
*.profraw
*.profdata
tarpaulin-report.html

# Temporary files
tmp/
temp/
*.tmp
*.bak
*.old

# OS-specific files
.DS_Store
Thumbs.db
desktop.ini

# Rust specific
**/*.rs.bk
Cargo.lock.bak

# Docker files (prevent recursive context)
Dockerfile*
docker-compose*.yml
.dockerignore

# Monitoring and deployment configs (not needed in image)
monitoring/
*.sh

# GitHub specific
.github/

# Local configuration (may contain secrets)
*.local
*.local.*
.env
.env.*
!.env.example

# Benchmark results
*.bench
criterion/

# Application data (should be mounted as volume)
data/
*.db
*.wal
